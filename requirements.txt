# Core Flask and web dependencies
Flask==2.3.3
Flask-CORS==4.0.0
Werkzeug==3.0.1

# Database and sync
psycopg[binary]>=3.1.0

# Image and video processing - essential only
Pillow>=10.0.0
opencv-python-headless==4.8.1.78
numpy>=1.24.0,<2.0.0

# AI/ML dependencies for Jetson
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0
transformers>=4.51.3
accelerate>=0.24.0
unsloth>=2024.1

# Audio processing - only what's actually used
SpeechRecognition>=3.10.0

# Vector search and embeddings
chromadb>=0.4.0
sentence-transformers>=2.2.0
timm>=0.9.0

# HTTP requests for API calls
requests>=2.31.0

timm
